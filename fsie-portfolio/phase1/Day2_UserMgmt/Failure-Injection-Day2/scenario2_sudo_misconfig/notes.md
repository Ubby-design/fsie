# Notes – Scenario 2

## Key Commands

```bash
sudo -l
tail -n 20 /var/log/auth.log

#Observations:

sudo denial recorded in logs.

Automation scripts fail when privileges revoked.

Principle of least privilege must be carefully managed.
